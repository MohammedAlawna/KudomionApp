<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="KudomionApp.MVVM.Views.ChatList"
             BackgroundColor="#1c1c1c"
             Title="ChatList">
    <ScrollView VerticalOptions="FillAndExpand">
        <StackLayout>
            <Border HeightRequest="100" StrokeThickness="0.2" Margin="10" BackgroundColor="#2e2e2e">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Grid.Column="0" Padding="3" Margin="0">
                        <StackLayout>
                            <Label Text="Chat 1" FontSize="18" />
                            <Label Text="Last message preview here..." FontSize="14" TextColor="White" />
                        </StackLayout>
                    </Border>
                </Grid>
            </Border>
        </StackLayout>
    </ScrollView>
</ContentPage>